{
  "version": 2,
  "builds": [{ "src": "vite.config.js", "use": "@vercel/static-build" }],
  "routes": [{ "src": "/(.*)", "dest": "/" }],
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
